# frozen_string_literal: true

RSpec.describe GraphitiScaffoldGenerator do
  it "has a version number" do
    expect(GraphitiScaffoldGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    skip 'TODO'
    expect(false).to eq(true)
  end
end
